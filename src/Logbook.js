import React, { Component } from "react";

class Logbook extends React.Component {
    render() {
        return (
            <div>pull in log entries here from wp api</div>
        );
    }
}

export default Logbook
